# 🗄️ Запуск миграций на Railway

## Вариант 1: Railway CLI (Рекомендуется)

1. **Установите Railway CLI:**
   ```bash
   npm i -g @railway/cli
   ```

2. **Войдите в Railway:**
   ```bash
   railway login
   ```

3. **Подключитесь к проекту:**
   ```bash
   railway link
   ```
   Выберите ваш проект из списка.

4. **Запустите миграции:**
   ```bash
   railway run npm run migrate
   ```

## Вариант 2: Через Railway Dashboard (Web Console)

1. Откройте **Railway Dashboard** → ваш проект
2. Выберите ваш сервис (backend service)
3. Перейдите на вкладку **"Deployments"**
4. Найдите последний деплой и нажмите на него
5. В правом верхнем углу найдите кнопку **"..."** (три точки) или **"Shell"**
6. Откройте **"Shell"** или **"Console"**
7. В открывшейся консоли выполните:
   ```bash
   npm run migrate
   ```

## Вариант 3: Через Settings → Variables

1. Railway Dashboard → ваш сервис
2. **Settings** → **Variables**
3. Найдите секцию **"Deploy"** или **"Commands"**
4. Если есть поле **"Build Command"** или **"Start Command"**, можно временно изменить на:
   ```
   npm run migrate && npm start
   ```
   Но это не рекомендуется для постоянного использования.

## Вариант 4: Добавить в start команду (временно)

Если другие варианты не работают, можно временно изменить `package.json`:

```json
{
  "scripts": {
    "start": "npm run migrate && node src/server.js"
  }
}
```

**⚠️ Внимание:** Это запустит миграции при каждом старте, что может быть медленно. Используйте только как временное решение.

## Проверка результата

После запуска миграций проверьте логи:
1. Railway Dashboard → Deployments → последний деплой → **View Logs**
2. Должно быть сообщение: `✅ Migrations completed successfully`

Или проверьте через Railway CLI:
```bash
railway logs
```

## Если миграции уже выполнены

Если миграции уже были выполнены ранее, команда `npm run migrate` просто пропустит их и выведет:
```
Already up to date
```

Это нормально - значит все миграции уже применены.

