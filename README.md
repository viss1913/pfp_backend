# PFP Service (Products & Portfolios)

Backend-сервис для управления финансовыми продуктами и инвестиционными портфелями.

## 🚀 Технологии

- **Node.js** (LTS) + **Express**
- **MySQL** (база данных)
- **Knex.js** (миграции и query builder)
- **Joi** (валидация)
- **OpenAPI 3.0** + **Swagger UI** (документация API)

## 📦 Установка

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository-url>
   cd backend-pfp
   ```

2. **Установите зависимости:**
   ```bash
   npm install
   ```

3. **Настройте переменные окружения:**
   
   Создайте файл `.env` в корне проекта:
   ```env
   DB_HOST=127.0.0.1
   DB_PORT=3306
   DB_USER=root
   DB_PASSWORD=your_password
   DB_NAME=pfp_service
   PORT=3000
   JWT_SECRET=your_secret_key
   ```

4. **Создайте базу данных:**
   ```sql
   CREATE DATABASE pfp_service CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   ```

5. **Запустите миграции:**
   ```bash
   npm run migrate
   ```

6. **Заполните начальные данные (опционально):**
   ```bash
   npm run seed
   ```

## 🎯 Запуск

### Development (с автоперезагрузкой)
```bash
npm run dev
```

### Production
```bash
npm start
```

Сервер запустится на `http://localhost:3000`

## 📚 Документация API

После запуска сервера откройте в браузере:

**Swagger UI:** `http://localhost:3000/api-docs`

### Основные эндпоинты

#### Products (Продукты)
- `GET /api/pfp/products` - Список продуктов
- `GET /api/pfp/products/:id` - Получить продукт
- `POST /api/pfp/products` - Создать продукт
- `PUT /api/pfp/products/:id` - Обновить продукт
- `DELETE /api/pfp/products/:id` - Удалить продукт
- `POST /api/pfp/products/:id/clone` - Клонировать дефолтный продукт

#### Portfolios (Портфели)
- `GET /api/pfp/portfolios` - Список портфелей
- `GET /api/pfp/portfolios/:id` - Получить портфель
- `POST /api/pfp/portfolios` - Создать портфель
- `PUT /api/pfp/portfolios/:id` - Обновить портфель
- `DELETE /api/pfp/portfolios/:id` - Удалить портфель
- `POST /api/pfp/portfolios/:id/clone` - Клонировать дефолтный портфель

#### Settings (Настройки системы)
- `GET /api/pfp/settings` - Список настроек
- `GET /api/pfp/settings/:key` - Получить настройку
- `POST /api/pfp/settings` - Создать настройку (admin)
- `PUT /api/pfp/settings/:key` - Обновить настройку (admin)
- `DELETE /api/pfp/settings/:key` - Удалить настройку (admin)

## 🔐 Аутентификация

Все запросы требуют заголовок:
```
x-agent-id: <agent_id>
```

Для административных операций дополнительно:
```
x-role: admin
```

## 🗄️ Структура базы данных

### Основные таблицы:
- `agents` - Агенты (справочник)
- `products` - Финансовые продукты
- `product_yields` - Доходность продуктов
- `portfolios` - Инвестиционные портфели
- `portfolio_classes` - Классы портфелей (Пенсия, Инвестиции и т.д.)
- `portfolio_risk_profiles` - Риск-профили (Conservative, Balanced, Aggressive)
- `portfolio_instruments` - Состав портфелей
- `system_settings` - Системные настройки

## 🛠️ Команды

```bash
npm start          # Запуск production-сервера
npm run dev        # Запуск development-сервера с nodemon
npm run migrate    # Применить миграции
npm run seed       # Заполнить тестовые данные
```

## 📝 Начальные данные

После выполнения `npm run seed` будут созданы:

### Продукт:
- **ПДС НПФ** (12% годовых, срок 0-100 месяцев)

### Портфели (для всех классов):
- Пенсия
- Пассивный доход
- Инвестиции
- Прочее

Каждый портфель содержит 3 риск-профиля (Conservative, Balanced, Aggressive), все с 100% ПДС.

### Системные настройки:
- Инфляция (0.33% в месяц)
- Рост расходов на инвестиции (0%)
- Параметры госпенсии (взносы, баллы, дожитие и т.д.)

## 🚢 Деплой на Railway

1. Подключите репозиторий к Railway
2. Добавьте MySQL плагин
3. Настройте переменные окружения:
   ```
   DB_HOST=${MYSQLHOST}
   DB_PORT=${MYSQLPORT}
   DB_USER=${MYSQLUSER}
   DB_PASSWORD=${MYSQLPASSWORD}
   DB_NAME=${MYSQLDATABASE}
   PORT=3000
   ```
4. Railway автоматически выполнит миграции при деплое

## 📄 Лицензия

ISC
