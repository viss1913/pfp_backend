# Абсолютная база API для ссылок на PDF из конструкторского site-chat (фронт на другом домене).
# Пример: https://pfpbackend-production.up.railway.app/api
# Если не задано — пробуется RAILWAY_PUBLIC_DOMAIN; без этого fallback-ссылка после расчёта не соберётся.
# PFP_PUBLIC_API_BASE_URL=

# Принудительно выставить agents.id для constructor_bots (и при необходимости project_id с карточки агента).
# Работает даже если у бота уже был другой agent_id — иначе env не применялся.
# Агент должен быть в том же project_id, что и бот (или у бота project_id пустой — подтянется с агента).
# CONSTRUCTOR_BACKFILL_AGENT_ID=10045

# --- LLM (OpenRouter): конструктор, Telegram, чаты и т.д. — см. src/services/aiService.js ---
OPENROUTER_API_KEY=
OPENROUTER_MODEL=google/gemma-3-27b-it
# ИИ в PDF-отчётах (Finam HTML/PDF, executive summary в JSON отчёта). 0 = только шаблоны.
# PFP_PDF_FINAM_AI=0
# Optional override for report executive summary generation
OPENROUTER_MODEL_SUMMARY=google/gemma-3-27b-it
# Optional override for risk profile explanation text generation
OPENROUTER_MODEL_RISK_PROFILE_EXPLANATION=google/gemma-3-27b-it
# Disable LLM generation for risk profile explanation (fallback-only mode)
# AI_RISK_PROFILE_EXPLANATION_ENABLED=true

MYSQLDATABASE=railway
MYSQLHOST=mysql.railway.internal
MYSQLPASSWORD=TOtYPvmAZPbRDViYPsQxdMxsnrUQByUm
MYSQLPORT=3306
MYSQLUSER=root
MYSQL_PUBLIC_URL=mysql://root:TOtYPvmAZPbRDViYPsQxdMxsnrUQByUm@maglev.proxy.rlwy.net:43619/railway

# Comon (опционально): override общего списка стратегий относительно COMON_BASE_URL / https://www.comon.ru
# Daily sync recommended на immers сам использует /api/v2/strategies + tags=recommended и не зависит от этого env.
# COMON_STRATEGIES_LIST_PATH=/api/v2/strategies
# COMON_SYNC_PAGE_SIZE=100
# COMON_SYNC_MAX_PAGES=3
# COMON_COOKIE=
# COMON_HTTP_TIMEOUT_MS=20000

# Kie.ai — GPT Image 2 и др. (https://docs.kie.ai/)
# KIE_API_KEY=

# Повторы при сбоях сети / 502–504 / 429 (число *дополнительных* попыток после первой; по умолчанию 2)
# COMON_HTTP_RETRIES=2
# Базовая задержка backoff (мс) между попытками: base * 2^attempt
# COMON_HTTP_RETRY_BASE_MS=500
# Исходящий HTTP CONNECT-прокси только для запросов к Comon (обход 403 с IP датацентра). Не путать с глобальным HTTP_PROXY.
# COMON_PROXY_URL=http://user:pass@your-proxy-host:3128
# Исходящий прокси только для Telegram Bot API (конструктор, polling). Не глобальный HTTP_PROXY.
# Локальный тест через SSH-туннель: ssh -D 127.0.0.1:10809 -N root@YOUR_VPS → socks5://127.0.0.1:10809
# Прод (Immers): HTTP-прокси на VPS, firewall только с IP Immers — см. docs/IMMERS_DEPLOY.md
# TELEGRAM_PROXY_URL=socks5://127.0.0.1:10809
# TELEGRAM_PROXY_URL=http://user:pass@45.77.80.63:3128
# На Immers предпочтительнее HTTP-прокси (:3128), SOCKS (:10809) иногда подвисает на sendMessage.
# TELEGRAM_API_TIMEOUT_MS=45000
# TELEGRAM_API_RETRIES=3
# TELEGRAM_CHAT_TASK_TIMEOUT_MS=120000
# Токен telegram-бота конструктора (скрипт scripts/upsert_telegram_constructor_bot.js → constructor_bots в БД)
# TELEGRAM_BOT_TOKEN=
# CONSTRUCTOR_BOT_AGENT_ID=2
# CONSTRUCTOR_BOT_PROJECT_ID=2
# История в промпте LLM конструктора: N записей constructor_logs (1 запись = 1 ход user+assistant ≈ 2 сообщения в чате)
# CONSTRUCTOR_CLASSIFIER_HISTORY_LOGS=20
# CONSTRUCTOR_GENERATOR_HISTORY_LOGS=20
# Ручной JSON-источник витрины Comon (массив стратегий или { items: [...] }).
# По умолчанию: data/comonRecommendedStrategies.json
# COMON_SHOWCASE_SOURCE_FILE=data/comonRecommendedStrategies.json
# Проекты, где вообще разрешена витрина Comon (по умолчанию Finam test/prod: 2,14)
# COMON_SHOWCASE_PROJECT_IDS=2,14

# --- AV Inform / Resolut (см. docs/partners/resolut/notes/RESOLUT_HYBRID_IMPLEMENTATION_NOTES.md) ---
# RESOLUT_BASE_URL=https://demo.avinfors.ru/pfp/api/pfp/
# RESOLUT_PROJECT_ID=23
# Таймаут HTTP к Resolut (мс): в коде ограничен диапазоном 8000–120000; значения ниже 8000 поднимаются до 8000.
# RESOLUT_TIMEOUT_MS=10000
# RESOLUT_STATIC_KEY=
# RESOLUT_SESSION_TTL_MS=82800000
# RESOLUT_NSJ_PFP_CODE=assetShort
# Проекты с PDF на HTML-шаблонах Finam (через запятую; по умолчанию 14,23,28,29).
# Immers test Finam project id=2: FINAM_REPORT_PROJECT_IDS=2 (см. docs/IMMERS_DEPLOY.md)
# FINAM_REPORT_PROJECT_IDS=14,23
# FINAM_REPORT_VERSION=2
# FINAM_REPORT_VERSION_PROJECT_IDS=2,14,23,28,29
# LIFE «Подушка безопасности» (тариф 1,44%): Immers test Finam id=2 в дефолте
# SBER_LIFE_CALC_PROJECT_IDS=2,14,28,29

# База URL страницы регистрации агента (invite-link, письмо субагенту). Query: project_key, ref, UTM.
# AGENT_REGISTER_BASE_URL=https://family-office.bank-future.com/register/
# FRONTEND_AGENT_REGISTER_URL=  # алиас
# Family Office magic-link (после POST .../family-office-invite). Если не задан — из AGENT_REGISTER_BASE_URL → /invite/activate
# AGENT_INVITE_ACTIVATE_BASE_URL=https://family-office.bank-future.com/invite/activate
# AGENT_INVITE_TOKEN_TTL_DAYS=7

# Finam ID платформы для FO self-register (POST /auth/register-family-office).
# Не пишется в agents.partner_agent_id (unique per project) — только effective/UTM в отчётах и ссылках.
# PFP_MAIN_FINAM_AGENT_ID=CM123456
# Опционально: разрешённые project_id для лендинга FO (через запятую). Пусто = любой валидный project_key.
# FAMILY_OFFICE_SELF_REGISTER_PROJECT_IDS=14,28

# --- Resend (транзакционная почта, см. src/services/emailService.js) ---
# API-ключ из https://resend.com/api-keys — в проде только через секреты хостинга/CI, не коммитить.
RESEND_API_KEY=
# Отправитель: адрес с верифицированного в Resend домена.
# Вариант 1 — общий ящик: PFP <noreply@bank-future.com>
# Вариант 2 — NDA с ящика агента на вашем домене (плейсхолдер {agent}):
#   RESEND_FROM_EMAIL={agent}@bank-future.com
#   Локальная часть: agents.email_corp (только ящик, без @domain), иначе из email агента, если домен совпадает; иначе agent_<id>.
# Для писем без агента (код регистрации) при шаблоне {agent} используется: RESEND_SYSTEM_LOCAL@тот_же_домен (по умолчанию noreply).
# RESEND_SYSTEM_LOCAL=noreply
# RESEND_FROM_EMAIL=
#
# Служебные письма (макро-синк и т.п.) — отдельный from, не {agent}@bank-future.com.
# RESEND_OPS_FROM_EMAIL=PFP <ops@bank-future.com>
# Immers/Docker: если NDA/отчёт падают с «could not be resolved» — задать IPv4 и DNS в compose (см. docker-compose.immers.example.yml).
# RESEND_API_IPV4=104.20.29.242
# RESEND_DNS_SERVERS=8.8.8.8,1.1.1.1
# Уведомления о синхронизации ИПЦ г/г (Excel ЦБ → russia_cpi_inflation_yoy). Через запятую.
# MACRO_SYNC_NOTIFY_EMAIL=ops@example.com
# MACRO_SYNC_NOTIFY_ENABLED=1
#
# HTTP-cron инфляции без shell (Railway Variables + POST /api/pfp/macro/cron/inflation):
# MACRO_CRON_SECRET=длинная-случайная-строка
# MACRO_INFLATION_STALE_DAYS=8
# MACRO_STARTUP_SYNC=0   — отключить авто-sync при старте, если данные старше N дней

# --- Financial news feed (Agent LK), см. GET /api/pfp/news/feed ---
# NEWS_ENABLED=true
# NEWS_CRON=*/15 * * * *
# NEWS_STORE_SCORE_MIN=25
# NEWS_PUBLISH_SCORE_MIN=60
# NEWS_FEED_DEFAULT_LIMIT=7
# NEWS_FEED_DEFAULT_HOURS=48
# NEWS_FEED_EXTENDED_HOURS=72
# NEWS_HTTP_TIMEOUT_MS=15000
# NEWS_PROVIDER_DELAY_MS=2500

#
# NDA: в «От» ещё и отображаемое ФИО консультанта (отключить: NDA_FROM_USE_AGENT_NAME=0).
# NDA_REPLY_TO_AGENT=1 — Reply-To на email агента (0 — выкл.).
# NDA: attach-url — PDF во вложении через R2 path (быстро на VPS). link — только ссылка. attach — base64 (медленно).
# NDA_EMAIL_DELIVERY=attach-url
# NDA_EMAIL_ATTACH_INLINE_MAX_BYTES=0
# REPORT_EMAIL_DELIVERY=attach
# Отчёт > N байт: вложение через R2 path (не base64 в JSON к Resend). Immers Finam ~2MB → 900000.
# REPORT_EMAIL_ATTACH_INLINE_MAX_BYTES=900000
# RESEND_ATTACH_ATTEMPTS=10

# Город в преамбуле NDA (см. src/services/ndaService.js), по умолчанию Москва.
# NDA_AGREEMENT_CITY=Москва

# NDA: URL публичной оферты и политики ПДн (подставляются в текст соглашения).
# NDA_PUBLIC_OFFER_URL=https://...
# NDA_PRIVACY_POLICY_URL=https://...
# (Не используется) раньше — реквизиты в таблице консультанта в PDF NDA.
# NDA_CONSULTANT_LEGAL_REQUISITES=

# --- PDF (Puppeteer): src/utils/renderHtmlToPdfBuffer.js ---
# Таймаут setContent/page.pdf (мс), в коде ограничен 15000–300000.
# REPORT_PDF_NAV_TIMEOUT_MS=120000
# Пауза после load перед печатью в PDF (мс, 0–5000). Finam v2 (инлайн-ассеты): ~80; остальное: 300.
# REPORT_PDF_POST_LOAD_DELAY_MS=300
# domcontentloaded | load | networkidle0 (Finam v2 по умолчанию domcontentloaded).
# REPORT_PDF_WAIT_UNTIL=domcontentloaded
# Параллельные вкладки Puppeteer на лист отчёта (1–16). Finam v2 дефолт 2, остальное 4.
# FINAM_REPORT_V2_RENDER_CONCURRENCY=2
# REPORT_PDF_RENDER_CONCURRENCY=4
# Кэш макро для страницы инфляции Finam (мс, дефолт 15 мин).
# FINAM_INFLATION_MACRO_CACHE_TTL_MS=900000
# Путь к Chrome/Chromium (иначе ищется по списку в коде).
# PUPPETEER_EXECUTABLE_PATH=

# --- SMM (опционально): синк агента во внешний SMM-сервис, см. src/services/smmService.js ---
# Без ключа вызовы sync просто no-op; в логах — короткое info, не ошибка.
# INTERNAL_API_KEY=
# или алиас:
# SMM_INTERNAL_API_KEY=
# SMM_API_URL=http://localhost:4000/api
